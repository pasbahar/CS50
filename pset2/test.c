#include <stdio.h>
#include <cs50.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main (int argc, string argv[])
{
int q;
string k=argv[];
for (i=0, n=strlen(k), i>=n-1, i++)
    {
    if (isupper(k[i]))
       printf("q=5");//(int) k[p] - 'A';
    if (islower(k[i]))
       ("q=4");//(int) k[p] - 'a';
    }
   
}





